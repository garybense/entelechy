{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "entelechy.fullname" . }}-api
  labels:
    {{- include "entelechy.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
  - port: {{ .Values.api.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "entelechy.api.selectorLabels" . | nindent 4 }}
{{- end }}
