{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "entelechy.fullname" . }}-postgresql
  labels:
    {{- include "entelechy.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.postgresql.service.port }}
    targetPort: postgresql
    protocol: TCP
    name: postgresql
  selector:
    {{- include "entelechy.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
{{- end }}
